const swiper = new Swiper('.swiper-container', {
    effect: 'fade',
    direction: 'vertical',
    autoplay: {
      delay: 2000,
    },
    slidesPerView: 1,
    loop: true,
    allowTouchMove: false,
});
